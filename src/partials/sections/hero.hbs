<section id="inicio">
    <div>
        <div>
            <span>{{hero_subtitle}}</span>
            <h1>{{hero_title}}</h1>
            <p>{{hero_description}}</p>

            <div>
                <a href="#servicios">{{hero_btn_primary}}</a>
                <a href="#contacto">{{hero_btn_secondary}}</a>
            </div>
        </div>

        <div>
            <img src="{{hero_img}}" alt="{{hero_title}}" />
        </div>
    </div>
</section>